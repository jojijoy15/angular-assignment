import { ProductsFilterPipe } from './filter.pipe';

describe('ProductsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
